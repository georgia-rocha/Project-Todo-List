import React from 'react';
import Input from './Input';

class Button extends React.Component {
    render() {
        return(
            <button id="criar-tarefa">Inserir</button>

        );
    }
}

export default Button;